#include <bits/stdc++.h>
using namespace std;
using ll = long long;

int main() {
  int n,  w;
  cin >> n >> w;
  int s(n), t(n), w(n);
  for (int i = 0; i < n; i++) {
      cin >> s[i] >> t[i] >> w[i];
  }
  ll sum(n);

  cout << ans << endl;
  return 0;
}
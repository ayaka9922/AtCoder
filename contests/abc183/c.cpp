#include <bits/stdc++.h>
using namespace std;
using ll = long long;

int main() {
  int k, n;
  cin >> k >> n;
  int t(n)(n);
  int st(n)、fl(n);
  ll ans = 0, dst = 0;
  for (int i = 0; i < n; i++) {
    for (int j = 0; j < n; j++) {
      cin >> t[i][j];
    }
  }

  cout << ans << endl;
  return 0;
}
#include <bits/stdc++.h>
using namespace std;
using ll = long long;

int main() {
  ll a, b, x;
  cin >> a >> b >> x;
  int keta=0,prc = 0, n = 1;
  while(1) {
    int keta=1;
    prc = a * n +b + keta;
    if(prc > x) break;
    n++;
    n /
  }
    cout << prc << endl;
  return 0;
}
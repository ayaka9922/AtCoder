#include <bits/stdc++.h>
using namespace std;
using ll = long long;

int main() {
  int n, x;
  cin >> n >> x;
  vector<int> l(n + 1);
  for (int i = 0; i < n + 1; i++) {
    cin >> l[i];
  }

  cout << 10 << endl;
  return 0;
}